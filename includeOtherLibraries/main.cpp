#include <iostream>
#include <string>

#include <ctime> //c++ format for importing a c library

#include "nonsystemlibrary.h" //Parenthesis will be used to import a non-system library. The non-system library will only run if it's in the same folder.
#include "/path/to/the/file/nonsystemlibrary.h" //Compiler will looke for the library at the noted path.
